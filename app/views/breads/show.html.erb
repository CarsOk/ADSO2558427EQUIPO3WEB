<div class="container">
  <h1 class="mt-4 mb-3">Detalles del Pan</h1>

  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= @bread.name %></h5>
      <p class="card-text"><strong>Descripción:</strong> <%= @bread.description %></p>
      <p class="card-text"><strong>Precio:</strong> <%= @bread.price %></p>

      <% if @bread.image.attached? %>
            <div class="image-container mb-3">
        <%= image_tag @bread.image, class: "img-fluid" %>
        </div>
      <% end %>

      <%= link_to 'volver a la lista', product_breads_path(@product), class: 'btn btn-primary' %>

    </div>
  </div>
</div>
