<div class="container">
  <h1 class="mt-4 mb-3">Detalles de la Dona</h1>

  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= @donut.name %></h5>
      <p class="card-text"><strong>Descripción:</strong> <%= @donut.description %></p>
      <p class="card-text"><strong>Precio:</strong> <%= @donut.price %></p>

      <% if @donut.image.attached? %>
        <div class="image-container mb-3">
          <%= image_tag @donut.image, class: "img-fluid" %>
        </div>
      <% end %>

      <%= link_to 'Volver a la lista', product_donuts_path(@product), class: 'btn btn-primary' %>
    </div>
  </div>
</div>