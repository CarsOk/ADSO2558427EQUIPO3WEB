<h1>Nuevo Pan</h1>

<%= form_with(model: @bread, url: product_breads_path(@product)) do |form| %>
  <%= form.hidden_field :product_id, value: @product.id %>
  <div class="form-group">
    <%= form.label :nombre, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control', required: true %>
    <% if @bread.errors.has_key?(:name) %>
      <p class="text-danger"><%= @bread.errors[:name].first %></p>
    <% end %>
  </div>

  <div class="form-group">
    <%= form.label :descripcion, class: 'form-label' %>
    <%= form.text_area :description, class: 'form-control', required: true %>
    <% if @bread.errors.has_key?(:description) %>
      <p class="text-danger"><%= @bread.errors[:description].first %></p>
    <% end %>
  </div>

  <div class="form-group">
    <%= form.label :precio, class: 'form-label' %>
    <%= form.number_field :price, class: 'form-control', required: true %>
    <% if @bread.errors.has_key?(:price) %>
      <p class="text-danger"><%= @bread.errors[:price].first %></p>
    <% end %>
  </div>

  <div class="form-group">
    <%= form.label :imagen, class: 'form-label' %>
    <%= form.file_field :image, class: 'form-control' %>
    <% if @bread.errors.has_key?(:image) %>
      <p class="text-danger"><%= @bread.errors[:image].first %></p>
    <% end %>
  </div>

  <div class="actions">
    <%= form.submit 'Guardar', class: 'btn btn-primary' %>
    <%= link_to 'Cancelar', product_breads_path(@product), class: 'btn btn-secondary' %>
  </div>
<% end %>