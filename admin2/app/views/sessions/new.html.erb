<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <!-- Agrega tus campos de formulario, etiquetas y botones necesarios -->
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.button :submit, "Iniciar sesión" %>
<% end %>